node:
  host: ${NODE_HOST}
  port: ${NODE_PORT}
  external: ${NODE_EXTERNAL_IP}

logging:
  level: ${LOGGING_LEVEL}

contracts:
  linkCollectionAddress : ${LINK_COLLECTION_ADDRESS}

validator:
  validator_key: ${VALIDATOR_KEY}

peers:
  - nodeId: ${PEER2_ID}
    endpoint: http://127.0.0.1:7071
  - nodeId: ${PEER3_ID}
    endpoint: http://127.0.0.1:7072
